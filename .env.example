# Optional public HTTPS origin for canonical and social-image URLs.
# Production defaults to https://3-am-philosophy.maryprtavian.workers.dev/.
# Set this in .env.local or the host's build variables only to override that address.
SITE_URL=
