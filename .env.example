# Optional public HTTPS origin for canonical and social-image URLs.
# After the first Direct Upload, set this in .env.local to the assigned pages.dev address.
# Cloudflare Git builds use CF_PAGES_URL automatically when this is blank.
SITE_URL=
